import { describe, it, vi } from 'vitest';
import { parseChallenge } from '../challenge.js';

vi.mock('sharp');

describe('challenge', () => {
  it('should ...', () => {
    console.log(parseChallenge);
  });
});
