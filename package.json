{
  "name": "@laboratoria/curriculum-parser",
  "version": "2.6.0",
  "license": "MIT",
  "bin": {
    "curriculum-parser": "./index.js"
  },
  "engines": {
    "node": ">=10"
  },
  "scripts": {
    "pretest": "eslint .",
    "test": "jest test/*.spec.js --verbose --coverage",
    "coveralls": "cat ./coverage/lcov.info | coveralls",
    "changelog": "git log $(git describe --tags --abbrev=0)..HEAD --oneline --format=\"* %h %s\""
  },
  "dependencies": {
    "@laboratoria/models": "^1.3.0",
    "@laboratoria/rubric": "^3.0.4",
    "chalk": "^4.1.0",
    "jsdom": "^16.4.0",
    "marked": "^1.2.3",
    "minimist": "^1.2.5",
    "mongoose": "^5.10.13",
    "semver": "^7.3.2"
  },
  "devDependencies": {
    "coveralls": "^3.1.0",
    "eslint": "^7.12.1",
    "eslint-config-airbnb-base": "^14.2.0",
    "eslint-plugin-import": "^2.22.1",
    "jest": "^26.6.3"
  }
}
